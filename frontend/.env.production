# The deployed frontend targets a locally-running backend.
# See README.md ("Try it live") for why: the backend isn't hosted, so
# viewers must clone the repo and start it themselves on port 5131.
REACT_APP_API_URL=http://localhost:5131
