export interface Author {
    name: string,
    birthDate: Date,
    nationality: string,
    id: number
}

export interface CreateAuthor {
    name: string,
    birthDate: Date,
    nationality: string
}